package com.company;

/**
 * Created by Nils on 19/02/2016.
 */
public class Trie {
    /*
    String word;
    char c;

    Node root;
    Node cur = root;
    for(c in word) {
        if(cur.next[c] == null) {
            cur.next[c] = new Node();
        }
        cur = cur.next[c];
    }
    cur.has = true;
    */

}
